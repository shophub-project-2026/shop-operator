---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: shops.shop.devops.io
spec:
  group: shop.devops.io
  names:
    kind: Shop
    listKind: ShopList
    plural: shops
    singular: shop
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              availability:
                type: string
              database:
                type: string
              image:
                type: string
              walletAddress:
                description: |-
                  Merchant's Ethereum address that customer payments are sent to.
                  Must be a 0x-prefixed 20-byte hex string. EIP-55 mixed case is
                  accepted; checksum is not enforced at admission time.
                pattern: ^0x[a-fA-F0-9]{40}$
                type: string
            required:
            - walletAddress
            type: object
          status:
            properties:
              databaseReady:
                type: boolean
              phase:
                type: string
              readyReplicas:
                format: int32
                type: integer
              replicas:
                format: int32
                type: integer
              serviceUrl:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
