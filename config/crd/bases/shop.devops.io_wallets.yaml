---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: wallets.shop.devops.io
spec:
  group: shop.devops.io
  names:
    kind: Wallet
    listKind: WalletList
    plural: wallets
    singular: wallet
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .spec.blockchain
      name: Blockchain
      type: string
    - jsonPath: .status.address
      name: Address
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              address:
                description: |-
                  Blockchain account to adopt. When empty the operator CREATES a
                  new account on the chain: it generates a keypair, stores it in
                  the Secret <wallet-name>-keys and publishes the derived address
                  in status.address.
                type: string
              blockchain:
                type: string
              currency:
                type: string
              network:
                type: string
            required:
            - blockchain
            type: object
          status:
            properties:
              address:
                description: |-
                  Account customers pay into — adopted from spec.address or
                  derived from the operator-generated keypair.
                type: string
              balance:
                type: string
              message:
                type: string
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
